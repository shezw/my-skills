## 场景

将 `agent-baseline` 调整回纯流程基线，剥离其中的语言专项内容，并完成本次修正的闭环记录。

### 开发目标

#### 1. 开发内容

- 移除 `agent-baseline` 中任何语言专项规范入口或引用。
- 删除 `agent-baseline` 下承载 Rust 语言内容的文件。
- 为本次任务补齐 `req`、`res`、`todo`、`tests`、`logs` 记录，并完成提交。

#### 2. 预期结果

- `agent-baseline` 仅保留开发流程与校验闭环，不再承载语言规范。
- Rust 规范仍保留在独立 `rust-standards` 技能中。
- 本次调整具备可追踪的测试、日志和提交记录。

#### 3. 验证方式

- 搜索 `agent-baseline/**`，确认不再出现 `Rust`、`rust-standards` 等语言专项内容。
- 检查 `agent-baseline/SKILL.md` 与工作流内容是否仍然完整且无诊断问题。
- 提交后读取最新一次提交信息，确认标题概括任务且正文列出核心改动点。