# 开发日志

- 时间戳: `202605012305`
- 场景: `react-guidelines`

## 开发内容

- 新增独立 `react-standards` 技能，覆盖 React 项目结构、组件边界、状态、测试、a11y 和性能规范。
- 新增默认 UI 栈说明，明确无特殊偏好时优先使用 Mantine 与 `@tabler/icons-react`。
- 新增评审清单和 feature 目录结构示例，帮助落干净模块拆分。
- 新增用户级 `react.instructions.md` 自动生效规则层。
- 为本次任务补齐 `req`、`res`、`todo`、`tests`、`logs` 记录。

## 问题与处理

- 问题: 需要在“没有特别偏好”的前提下给出一套既成熟稳健、又不会强迫项目过度自建 UI 基础设施的 React 默认规范。
- 处理: 采用独立技能加自动生效规则层的结构，并将默认 UI 栈收敛到 Mantine + Tabler Icon，同时补足状态、表单、测试、a11y 和性能门槛。

## 验证

- 已确认 `react-standards/` 的技能文件、规范正文、默认栈说明、评审清单和目录示例均无诊断问题。
- 已确认用户级 `react.instructions.md` 存在，且覆盖 React 组件文件。
- 已确认规范文本包含 Mantine、Tabler、feature 拆分、hooks、表单、测试、可访问性和性能关键条目。
- 待本次提交完成后，读取最新提交信息并回写测试结果。

## 提交

- 提交状态: `committed`
- 提交信息标题: `skills: 添加 React 项目规范`
- 核心改动点:
- 新建独立 `react-standards` 技能并补齐规范正文、默认栈与评审清单
- 默认 UI 栈采用 Mantine 与 `@tabler/icons-react`
- 补齐本次 React 规范任务的 req、res、todo、tests、logs 记录

## Git 状态

- 当前仓库提交范围包含 `react-standards/` 和本次 `.agent` 记录。
- 用户级 `react.instructions.md` 位于仓库外部，不包含在当前仓库提交中。