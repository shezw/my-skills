# 测试用例

- 测试 ID: `3`
- 项目: `skills`
- 场景: `rust-guidelines`
- 目标: `rust-instruction`
- 创建时间: `2026-05-01 22:13:20`

## 测试验证

1. 检查用户级 Rust 指令文件 frontmatter 是否完整，并确认 `applyTo` 为 `**/*.rs`。
2. 检查镜像文档是否存在，并确认包含模块深度不超过 3 层、文件行数建议、`//!`、`///`、`unsafe`、错误处理、测试和 `cargo clippy` 等条目。
3. 检查本次任务的 `req`、`res`、`todo` 记录是否存在。
4. 完成 git 提交后，读取最新提交信息，确认标题概括任务且正文列出核心改动点。

## 结果判定

- 返回值: `true | false`
- 通过条件: Rust 指令文件和镜像文档存在，关键规则完整，且本次变更已通过符合规范的提交写入 git 历史。
- 失败处理: 将 `results.db` 中 ID 为 `3` 的记录 `fails` 加 `1`。

## 执行记录

- 最近结果: `true`
- 说明: 已验证用户级 Rust 指令与镜像文档覆盖关键规则，并确认提交 `cc6b4616b37c0baaa27406b89b3283f4312a4b08` 的标题和正文符合默认提交规范。