# 响应记录

- 时间戳: `202605012250`
- 问题: `如何把语言专项规范从 baseline 中彻底剥离，并避免再次混入流程技能`

## 结论

`agent-baseline` 将回到纯流程基线职责，只保留 `.agent` 闭环、测试回写、日志和提交流程；语言规范只保留在独立技能和对应的用户级指令中。

## 关键决策

- 移除 `agent-baseline/SKILL.md` 和工作流中的语言专项入口。
- 删除 `agent-baseline/references/rust-code-standards.md`，避免 baseline 目录继续承载任何语言内容。
- 将“流程技能不承载语言规范”作为后续 customization 的边界建议明确下来。

## 后续动作

- 校验 `agent-baseline` 目录中已不再包含 Rust 或语言专项内容。
- 完成本次任务日志、测试和提交。