# 响应记录

- 时间戳: `202605012322`
- 问题: `如何为同时支持单体和微服务的 Go 项目制定一套成熟、可读、不过分庞杂且 AI 友好的规范`

## 结论

本次会创建独立的 `go-standards` 技能，并补一份用户级 `go.instructions.md` 作为对 `*.go` 文件默认生效的规则层。

## 关键决策

- Go 规范保持为独立技能，不进入 `agent-baseline`。
- 核心骨架采用清洁架构、依赖倒置、配置驱动运行模式和事件优先。
- 在用户给出的八条基础上，仅补充少量高价值工程项，例如目录约定、`context.Context` 传播、事务边界、测试和生成代码边界。

## 后续动作

- 创建 Go 技能、规范正文、架构默认项、评审清单和目录结构示例。
- 创建用户级自动生效规则层并完成验证、日志和提交。