# 响应记录

- 时间戳: `202605012244`
- 问题: `如何把 Rust 规范从基线技能中的孤立文档调整为正确的技能结构，并暴露给基线技能`

## 结论

本次会将 Rust 规范重构为独立的 `rust-standards` 技能，并保留用户级 `rust.instructions.md` 作为对 `*.rs` 自动生效的规则层。

## 关键决策

- 仓库内标准文档迁移到独立技能目录，避免继续挂在 `agent-baseline/references/` 下造成职责混淆。
- `agent-baseline/SKILL.md` 和工作流会显式暴露 Rust 入口，保证语言专项规范可发现。
- 旧路径保留兼容转向页，避免已有手工引用失效。

## 后续动作

- 创建独立 Rust 技能与配套检查清单。
- 更新基线技能入口并验证技能结构、引用和提交记录。