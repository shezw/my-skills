# 响应记录

- 时间戳: `202605012305`
- 问题: `如何为 React 项目建立一套默认优秀、可读性强、模块拆分干净且偏成熟生态的通用规范`

## 结论

本次会创建独立的 `react-standards` 技能，并补一份用户级 `react.instructions.md` 作为对 React 组件文件默认生效的规则层。

## 关键决策

- React 规范保持为独立技能，不进入 `agent-baseline`。
- 没有特殊偏好时，默认采用 `Mantine + @tabler/icons-react` 作为 UI 基础栈。
- 规范除模块拆分和可读性外，还补齐状态管理、数据获取、表单、可访问性、测试和性能等高价值工程约束。

## 后续动作

- 创建 React 技能、规范正文、评审清单和推荐目录结构示例。
- 创建用户级自动生效规则层并完成验证、日志和提交。